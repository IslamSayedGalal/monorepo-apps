{
  "watch": ["src", "../../libs"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "node_modules", "dist"],
  "exec": "ts-node --project tsconfig.app.json -r tsconfig-paths/register src/main.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
